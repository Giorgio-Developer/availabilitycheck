<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Errore</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        body {
            background-color: #121212;
            color: #03a9f4;
        }
        h1, p {
            text-align: center;
        }
        .container {
            margin-top: 5rem;
        }
        .error-container {
            background-color: #1f1f1f;
            color: #03a9f4;
            border: 1px solid #03a9f4;
            padding: 30px;
            border-radius: 10px;
            max-width: 600px;
            margin: auto;
        }
        .btn-primary {
            background-color: #03a9f4;
            border-color: #03a9f4;
            color: white;
        }
        .btn-primary:hover {
            background-color: #0288d1;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="error-container">
            <h1>Errore</h1>
            <p><%= message %></p>
            <div style="text-align: center;"><a href="<%= backUrl %>" class="btn btn-primary">Indietro</a></div>
        </div>
    </div>
</body>
</html>
